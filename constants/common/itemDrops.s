; These are subid values for PART_ITEM_DROP.
; Also use these in the editor when making item drop objects.

.enum 0
	ITEM_DROP_FAIRY                   db ; $00
	ITEM_DROP_HEART                   db ; $01
	ITEM_DROP_1_ORE_CHUNK             db ; $02
	ITEM_DROP_10_ORE_CHUNKS           db ; $03
	ITEM_DROP_BOMBS                   db ; $04
	ITEM_DROP_0a                      db ; $0a
	ITEM_DROP_0b                      db ; $0b
	ITEM_DROP_1_RUPEE                 db ; $0c
	ITEM_DROP_5_RUPEES                db ; $0d
	ITEM_DROP_50_ORE_CHUNKS           db ; $0e
	ITEM_DROP_100_RUPEES_OR_ENEMY     db ; $0f
.ende
